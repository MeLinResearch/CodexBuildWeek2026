# Release Assurance Frontend

Fixture-backed React application powered by Bun, Vite, TanStack Router and Query, Tailwind CSS v4, shadcn/Base UI, and a local Hono mock server.

```bash
bun install --frozen-lockfile
bun dev
```

Use `bun lint`, `bun test`, and `bun build` for verification.
